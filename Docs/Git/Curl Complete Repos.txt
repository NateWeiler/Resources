							#######################
							# Curl Complete Repos #
							#######################
# User Repo #
curl "https://api.github.com/users/##GHUSER##/repos?per_page=1000" | grep -o 'git@[^"]*' | xargs -L1 git clone

# Org Repo #
curl "https://api.github.com/orgs/##GHORG##/repos?per_page=1000" | grep -o 'git@[^"]*' | xargs -L1 git clone

####################################################################################

curl "https://api.github.com/users/NAME/repos?page=1&per_page=1000" | grep -e 'git_url*' | cut -d \" -f 4 | xargs -L1 git clone --recursive --recurse-submodules

curl -s "https://api.github.com/orgs/#GHORG#/repos?per_page=1000&type=all" | jq -r '.[].html_url' | xargs -L1 git clone

curl "https://api.github.com/orgs/#GHORG#/repos?per_page=1000" | grep -o 'git@[^"]*' | xargs -L1 git clone

curl -s https://api.github.com/orgs/#GHORG#/repos?per_page=1000 | -r -rubygems -e 'require "json"; JSON.load(STDIN.read).each { |repo| %x[git clone #{repo["ssh_url"]} ]}'
